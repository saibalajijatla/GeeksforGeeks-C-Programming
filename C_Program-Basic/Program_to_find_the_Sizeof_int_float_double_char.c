#include<stdio.h>
int main()
{
    int intergertype;
    char chartype;
    float floatType;
    double doubleType;

    printf("Size of int is: %d", sizeof(intergertype));
    printf("\nSize of char is %ld", sizeof(chartype));
    printf("\nSize of float is %ld", sizeof(floatType));
    printf("\nSize of doubleType is %ld", sizeof(doubleType));

    return 0;

}